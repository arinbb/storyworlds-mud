# New MUD Zone Creation
1. Ask for zone name, theme, and approximate room count
2. Create zone directory structure
3. Generate rooms with valid YAML (validate each file)
4. Add spawn points, containers, and mob definitions
5. Run full YAML validation on all new files
6. Test zone loads without errors before committing
